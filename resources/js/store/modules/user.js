const state = {
    user: null,
    userStatus: null,
};

const getters = {
    authUser: state => {
        return state.user;
    }
};

const actions = {

}

const mutations = {

}

export default {
    state, getters, actions, mutations,
}
